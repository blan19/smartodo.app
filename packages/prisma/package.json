{
  "name": "@smartodo/prisma",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "db:migrate": "npx prisma migrate dev --name user-entity --create-only && npx prisma generate",
    "db:push": "npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^4.13.0",
    "prisma": "^4.13.0"
  },
  "main": "index.ts",
  "types": "index.d.ts"
}
